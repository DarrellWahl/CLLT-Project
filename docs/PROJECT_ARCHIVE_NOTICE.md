# Archive Notice

This optional helper note clarifies use of `docs/archive/`.

- Keep current canonical docs in `docs/` root.
- Move superseded versions and one-off analysis files into `docs/archive/` with timestamped names.
- Do not store active project state exclusively in archive files.
