# Project History

Document version: 1.0.0  
Snapshot at (UTC): 2026-07-07T14:15:25Z  
Generated by: GitHub Copilot (GPT-5.3-Codex)

## Timeline Overview

This history reflects the major implementation and debugging sequence that led to current project state.

## Phase 1: Deterministic Pipeline Continuation

- Objective: continue and stabilize deterministic extraction.
- Work focused on verifying extraction counts and exported artifacts.
- Initial discrepancy analysis surfaced path confusion between output directories.

Outcome:

- Clarified that count mismatches were driven by mixed old/new artifacts and non-canonical paths.

## Phase 2: Completeness Verification Against User Expectations

- User repeatedly challenged under-extraction and requested actual count validation.
- Pipeline and website artifacts were reconciled to full extracted candidate set.

Outcome:

- Website dataset moved to 1,090 record basis.

## Phase 3: Frontend Runtime Incident (Page Unresponsive)

Incident:

- Rendering large card sets synchronously caused browser unresponsiveness.

Fixes:

- Batched rendering with chunking and `requestAnimationFrame`.
- “Load more” incremental list growth.
- Lazy loading via `IntersectionObserver`.

Outcome:

- UI recovered responsiveness under large dataset conditions.

## Phase 4: Lazy-Load Reliability Defect

Incident:

- Image source handling had a broken condition path related to empty `src` assignment.

Fixes:

- Reworked lazy-load checks to use attribute-aware logic.
- Corrected image src initialization behavior.

Outcome:

- Cards consistently displayed images without silent failures.

## Phase 5: Label Coverage Expansion

- Attempted visual-match propagation first; reliability was insufficient.
- Established Python 3.11 OCR-specific environment (`.venv311`).
- Ran OCR labeling pass over sign set.

Outcome:

- Recovered 258 coded records in website dataset.

## Phase 6: Semantic Label Quality Concerns

Incident:

- User identified mismatched code/description semantics in examples.

Fixes:

- Targeted corrections applied for known problematic cluster.
- Frontend cache-bust increments ensured latest script/data loading.

Outcome:

- Some obvious drifts corrected, but full semantic validation remains pending.

## Phase 7: Documentation Pivot (Current)

- User requested complete permanent dossier under `docs/`.
- Constraint: inspect/analyze/document only; do not modify source code.
- Performed inventory and script archaeology, then generated this document suite.

Outcome:

- Required dossier files created and cross-referenced.

## Historical Lessons

1. Count discrepancies are often path-governance issues, not only detection-model issues.
2. Frontend scalability must be designed for worst-case dataset size from day one.
3. OCR coverage gains are not equivalent to semantic correctness.
4. Cache invalidation is operationally critical for web troubleshooting.
5. Documentation artifacts should be generated continuously, not only at handoff time.
